{
  "name": "gauge-taiko-template",
  "description": "Starter template for writing JavaScript tests for Gauge",
  "scripts": {
    "test": "gauge run specs/",
    "website-test": "gauge run --env headless --parallel -n=2 specs/website/google.spec"
  },
  "dependencies": {
    "taiko": "latest"
  }
}
